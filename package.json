{
  "name": "slack-app",
  "version": "1.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "shared": "dotenv -e .env yarn workspace @slack-app/shared",
    "server": "dotenv -e .env yarn workspace @slack-app/server",
    "web": "PORT=3001 dotenv -e .env yarn workspace @slack-app/web",
    "lint": "eslint . --fix"
  },
  "author": "",
  "license": "ISC",
  "engines": {},
  "dependencies": {},
  "devDependencies": {
    "@slack-app/eslint-config": "*",
    "dotenv-cli": "^6.0.0"
  }
}
